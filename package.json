{
  "name": "@xcvzmoon/xcvz-enesti",
  "description": "Nitro Starter Template",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "format": "prettier --write server/",
    "lint": "oxlint . --fix -D correctness --ignore-path .gitignore"
  },
  "devDependencies": {
    "nitropack": "latest",
    "oxlint": "^1.9.0",
    "prettier": "^3.6.2"
  }
}
